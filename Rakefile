require File.expand_path('../config/ruby_mud', __FILE__)

# TODO: same as another todo in config/ruby_mud.rb - why not use expand_path?
Dir[File.join(__dir__, 'lib', 'tasks/*.rake')].each { |file| import file }
Dir[File.join(__dir__, 'lib', 'tasks/**/*.rake')].each { |file| import file }
